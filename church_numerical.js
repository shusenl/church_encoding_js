//use arrow function definition in ES6
var zero = (x) => x;

// short cut, used for which is outside the minimal subset
var number = (n) => { (f) => {return function(z) { for(var i=0; i<n; i++) z = f(z); return z;}  }  };

var TRUE = (a b) => a;
var FALSE  = (a b) => b;


